source "https://rubygems.org"

# Pin to a stable minor version once the first successful build is confirmed.
# Update the constraint deliberately when upgrading (not silently via dep resolution).
gem "fastlane", "~> 2.226"

# Plugins (uncomment as needed)
# gem "fastlane-plugin-versioning"  # auto-increment build numbers
